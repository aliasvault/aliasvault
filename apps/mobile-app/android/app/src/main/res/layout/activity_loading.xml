<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/av_background">

    <!-- Loading state -->
    <include
        android:id="@+id/loadingIndicator"
        layout="@layout/loading_indicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="160dp" />

    <!-- Error state -->
    <LinearLayout
        android:id="@+id/errorContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:orientation="vertical"
        android:padding="32dp"
        android:gravity="center"
        android:visibility="gone">

        <TextView
            android:id="@+id/errorMessage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/av_text"
            android:textSize="16sp"
            android:gravity="center"
            android:lineSpacingMultiplier="1.3" />

        <!-- Collapsible technical error detail. Hidden by default. -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/errorDetailsToggle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="@string/common_show_details"
            android:textColor="@color/av_primary"
            style="@style/Widget.Material3.Button.TextButton"
            android:textSize="14sp"
            android:visibility="gone" />

        <LinearLayout
            android:id="@+id/errorDetailsContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:orientation="vertical"
            android:visibility="gone">

            <ScrollView
                android:layout_width="match_parent"
                android:layout_height="200dp"
                android:background="@color/av_accent_background"
                android:scrollbars="vertical">

                <TextView
                    android:id="@+id/errorDetailsText"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:padding="12dp"
                    android:textColor="@color/av_text"
                    android:textSize="12sp"
                    android:textIsSelectable="true"
                    android:fontFamily="monospace"
                    android:gravity="start" />

            </ScrollView>

            <com.google.android.material.button.MaterialButton
                android:id="@+id/errorDetailsCopyButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="8dp"
                android:text="@string/common_copy"
                android:textColor="@color/av_primary"
                style="@style/Widget.Material3.Button.TextButton"
                android:textSize="14sp" />

        </LinearLayout>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/closeButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="@string/common_close"
            android:textColor="@color/av_primary"
            style="@style/Widget.Material3.Button.TextButton"
            android:paddingVertical="14dp"
            android:textSize="16sp" />

    </LinearLayout>

</RelativeLayout>
