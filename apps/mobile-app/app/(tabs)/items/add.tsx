import AddEditItemScreen from '@/components/items/AddEditItemScreen';

/**
 * Create item screen, shown as a modal.
 */
export default function AddItemScreen(): React.ReactNode {
  return <AddEditItemScreen />;
}
