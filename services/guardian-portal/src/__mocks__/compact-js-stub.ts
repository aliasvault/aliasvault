/**
 * Stub for @midnight-ntwrk/compact-js which is not installed locally
 * (private registry). vi.mock() overrides this in tests.
 */
export const CompiledContract = {
  make: () => ({ pipe: (fn: unknown) => fn }),
  withWitnesses: (w: unknown) => w,
};
